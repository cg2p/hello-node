apiVersion: route.openshift.io/v1
kind: Route
metadata:
  labels:
    app: hello-node-app
  name: hello-node-app
spec:
  port:
    targetPort: 3000-tcp
  to:
    kind: Service
    name: hello-node-app
    weight: 100